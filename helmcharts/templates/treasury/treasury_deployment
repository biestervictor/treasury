apiVersion: apps/v1
kind: Deployment
metadata:
  name: treasury
  namespace: {{ .Values.treasury.nameSpace}}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: treasury
  template:
    metadata:
      labels:
        app: treasury
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: treasury
          image: ghcr.io/biestervictor/treasury:e0933110b6b650ea3b3ccc700a38b99ffdcd1466
          command: ["java", "-jar", "app.jar"]
          args: ["--spring.profiles.active=kubitos"]
          ports:
            - containerPort: 8080