apiVersion: apps/v1
kind: Deployment
metadata:
  name: treasury
  namespace: {{ .Values.treasury.nameSpace}}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: treasury
  template:
    metadata:
      labels:
        app: treasury
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: treasury
          image: ghcr.io/biestervictor/treasury:769a63503576ad5c170b872dea434243238a4f63
          command: ["java", "-jar", "app.jar"]
          args: ["--spring.config.name=application-kubitos"]
          ports:
            - containerPort: 8080